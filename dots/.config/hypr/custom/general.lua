-- Custom general configuration overrides
-- These override the defaults in hyprland/general.lua

-- Monitor config (auto-detect with preferred settings)
-- hl.monitor({ output = "DP-1", mode = "3440x1440@165" })
-- hl.config({ misc = { vrr = 3 } })

-- Input config: GB keyboard layout
-- hl.config({ input = { kb_layout = "gb" } })
